{
  "name": "contrabus-proxy",
  "version": "1.0.0",
  "description": "Proxy server for contrabus API",
  "main": "server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "test": "node --test tests/**/*.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^8.2.0"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "terser": "^5.48.0"
  }
}
